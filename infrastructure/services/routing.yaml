apiVersion: v1
kind: ServiceRouting
metadata:
  name: backend-routing-config
spec:
  services:
  - name: gsmrsvd-rzpa7sugpfgdc5rx7tnx4r-1g2hoa49
    routing:
      urlMap: ${SERVICE}-urlmap
      targetProxy: ${SERVICE}-proxy
      healthCheck:
        path: /health
        port: 8080
  # Repeat for all 14 services